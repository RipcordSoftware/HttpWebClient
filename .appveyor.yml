platform: Any CPU
image: Visual Studio 2017

before_build:
    - nuget restore
    - nuget install OpenCover -OutputDirectory buildtools
    - nuget install xunit.runner.console -OutputDirectory buildtools

build_script:
    - msbuild /p:Configuration=Debug HttpWebClient.sln

    